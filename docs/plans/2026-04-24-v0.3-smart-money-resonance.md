# v0.3 Smart Money 与共振信号 Implementation Plan

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** 为庄家雷达接入 Binance Web3 / 链上聪明钱数据，并产出 `🧠 链上聪明钱榜` 与 `🧬 链上链下共振榜`。

**Architecture:** v0.3 不重写现有 v0.2 扫描链路，而是在现有 `MarketFeature -> RadarSignal -> formatter -> Telegram` 流程旁边新增 Smart Money 数据模型、采集器、评分器与合并器。链上信号先做“可选增强”：采集失败时不影响 OKX/Binance 合约雷达正常播报。

**Tech Stack:** Python 3.12, stdlib `urllib`, dataclasses, SQLite, pytest, existing Binance/OKX collectors, existing Telegram service.

---

## Scope

### In Scope

- 新增 Smart Money 数据模型。
- 新增 Binance Web3 collector 封装，优先通过本机已安装的 Binance Web3 组件/CLI 调用。
- 新增 Token Audit 风险过滤模型。
- 新增 Meme Rush / 热度输入的轻量模型。
- 新增链上信号评分：`链上聪明钱`、`鲸鱼介入`、`已出货风险`。
- 新增跨源共振合并：合约信号 + 链上信号同标的合并为 `链上链下共振`。
- Formatter 新增两个榜单：`🧠 链上聪明钱榜`、`🧬 链上链下共振榜`。
- SQLite 存储 signal metadata，便于 v0.4 回测使用。
- CLI 支持 `--no-smart-money` 和配置开关。
- Telegram 自动推送沿用现有服务，不改 systemd 单元。

### Out of Scope

- 不做自动交易。
- 不做链上地址全量追踪。
- 不引入付费 API。
- 不做 v0.4 回测统计，只为回测预留字段。
- 不强依赖 Binance Web3：失败时降级为 v0.2 行为。

---

## Acceptance Criteria

- `python3 -m pytest -q` 全部通过。
- `banker-radar scan --db data/v03-test.db` 在 Smart Money 不可用时仍正常输出 v0.2 风格报告。
- `banker-radar scan --db data/v03-test.db --no-smart-money` 明确跳过链上模块。
- mock Smart Money 数据时，报告包含：
  - `🧠 链上聪明钱榜`
  - `🧬 链上链下共振榜`
- 同一 symbol 同时存在 OKX/Binance 合约异动与 Smart Money buy 时，最终输出只保留合并后的高分共振信号，避免重复刷屏。
- 高风险 Token Audit 标的不进入正向聪明钱榜，只能进入风险提示或被过滤。
- `.env`、数据库、token 不入库。

---

## Proposed Config

Modify `configs/radar.yaml`:

```yaml
smart_money:
  enabled: true
  limit: 30
  min_score: 60
  min_smart_wallets: 2
  max_exit_rate_pct: 50
  max_gain_pct_for_fresh_signal: 80
  allowed_chains:
    - bsc
    - solana
    - ethereum
  audit:
    enabled: true
    block_high_risk: true
  meme_rush:
    enabled: true
    heat_bonus_max: 10

resonance:
  enabled: true
  symbol_match_min_score: 70
  bonus_for_contract_signal: 15
  bonus_for_smart_money: 20
  bonus_for_heat: 8
```

---

## Data Model Design

Add to `src/banker_radar/models.py`:

```python
@dataclass(frozen=True)
class SmartMoneyFeature:
    symbol: str
    chain: str
    token_address: str
    side: str  # buy | sell
    smart_wallet_count: int
    whale_count: int
    trigger_price: float
    current_price: float
    max_gain_pct: float
    exit_rate_pct: float
    volume_usd: float
    tags: tuple[str, ...] = ()
    source: str = "binance-web3"

@dataclass(frozen=True)
class TokenAuditResult:
    symbol: str
    chain: str
    token_address: str
    risk_level: str  # low | medium | high | unknown
    is_honeypot: bool = False
    can_sell: bool = True
    reason: str = ""

@dataclass(frozen=True)
class HeatFeature:
    symbol: str
    score: int
    reason: str
    source: str = "meme-rush"
```

Extend `RadarSignal` minimally:

```python
metadata_json: str = ""
```

Migration note: `RadarSignal` currently has positional construction in `scoring.py`; add the new field at the end with default so old calls remain compatible.

---

## Task 1: Add v0.3 Models

**Objective:** Add typed models for Smart Money, token audit, heat signals, and metadata-compatible `RadarSignal`.

**Files:**
- Modify: `src/banker_radar/models.py`
- Test: `tests/test_models_v03.py`

**Steps:**
1. Write tests that instantiate `SmartMoneyFeature`, `TokenAuditResult`, `HeatFeature`, and `RadarSignal(metadata_json="{}")`.
2. Run: `python3 -m pytest tests/test_models_v03.py -q` and confirm failure.
3. Add dataclasses to `models.py`.
4. Add `metadata_json: str = ""` to `RadarSignal` and preserve `with_timestamp()`.
5. Run: `python3 -m pytest tests/test_models_v03.py tests/test_signals.py -q`.
6. Commit: `feat: add v0.3 smart money models`.

---

## Task 2: Add Binance Web3 Collector Interface

**Objective:** Create a collector that can parse Smart Money JSON rows from the Binance Web3 component while remaining testable without live network.

**Files:**
- Create: `src/banker_radar/collectors/binance_web3.py`
- Test: `tests/test_binance_web3_collector.py`

**Design:**

Expose pure parser functions first:

```python
def parse_smart_money_rows(rows: list[dict]) -> list[SmartMoneyFeature]: ...
def parse_token_audit_row(row: dict) -> TokenAuditResult: ...
def parse_heat_rows(rows: list[dict]) -> list[HeatFeature]: ...
```

Then expose live functions with safe failure:

```python
def fetch_smart_money(limit: int = 30) -> list[SmartMoneyFeature]: ...
def fetch_token_audit(symbol: str, chain: str, token_address: str) -> TokenAuditResult | None: ...
def fetch_meme_heat(limit: int = 30) -> list[HeatFeature]: ...
```

**Implementation guidance:**
- Prefer invoking installed Binance Web3 components through a small subprocess wrapper if no importable SDK exists.
- Return `[]` on collector failure and log a short warning; do not crash `scan`.
- Normalize symbols to `XXXUSDT` where possible so they can match Binance futures symbols.

**Steps:**
1. Write parser tests with sample raw rows containing smart buy, whale buy, sell, exitRate, maxGain, tags.
2. Implement parsers only.
3. Add live functions as thin wrappers that can be monkeypatched in tests.
4. Run: `python3 -m pytest tests/test_binance_web3_collector.py -q`.
5. Commit: `feat: add binance web3 collector`.

---

## Task 3: Add Smart Money Scoring

**Objective:** Convert Smart Money features into `RadarSignal` objects.

**Files:**
- Create: `src/banker_radar/signals/smart_money.py`
- Test: `tests/test_smart_money_scoring.py`

**Rules v0.3:**

1. `链上聪明钱`
   - `side == "buy"`
   - `smart_wallet_count >= min_smart_wallets`
   - `exit_rate_pct <= max_exit_rate_pct`
   - audit not high risk

2. `鲸鱼介入`
   - same as buy signal
   - `whale_count >= 1` or tags include `Whale Buy`

3. `已出货风险`
   - `side == "sell"` OR `exit_rate_pct > max_exit_rate_pct`

4. Score formula:

```text
base 45
+ smart_wallet_count * 6, max 24
+ whale_count * 10, max 20
+ fresh bonus if max_gain_pct <= max_gain_pct_for_fresh_signal, max 10
+ heat bonus, max configured
- audit penalty for medium/unknown risk
```

**Steps:**
1. Write tests for buy, whale buy, high exit risk, high audit block.
2. Implement `score_smart_money(feature, audit=None, heat=None, cfg=None)`.
3. Include metadata JSON with chain, token address, exit rate, max gain, tags.
4. Run: `python3 -m pytest tests/test_smart_money_scoring.py -q`.
5. Commit: `feat: score smart money signals`.

---

## Task 4: Add Cross-Source Resonance Engine

**Objective:** Merge market signals and smart money signals into stronger resonance signals and remove duplicates.

**Files:**
- Create: `src/banker_radar/signals/resonance.py`
- Test: `tests/test_resonance.py`

**Rules v0.3:**

- Normalize symbols:
  - `KAT-USDT-SWAP` -> `KATUSDT`
  - `KATUSDT` -> `KATUSDT`
- If a market signal and a smart signal share normalized symbol:
  - create `RadarSignal(kind="链上链下共振")`
  - score = max(market.score, smart.score) + configured bonuses, clamped to 100
  - risk = max risk of both, but high audit risk must not become positive resonance
  - reason = compact combination of both reasons
  - source = `market+binance-web3`
- Do not output the original two signals if resonance is emitted, unless `debug_keep_components` is configured.

**Steps:**
1. Write tests for symbol normalization.
2. Write tests for merge and duplicate suppression.
3. Implement `normalize_symbol()` and `merge_resonance(market_signals, smart_signals, cfg)`.
4. Run: `python3 -m pytest tests/test_resonance.py -q`.
5. Commit: `feat: add chain contract resonance engine`.

---

## Task 5: Extend SQLite Storage With Metadata

**Objective:** Store metadata JSON without breaking existing databases.

**Files:**
- Modify: `src/banker_radar/storage/sqlite.py`
- Test: `tests/test_storage_v03.py`

**Migration approach:**

After `CREATE TABLE IF NOT EXISTS signals`, inspect columns with `PRAGMA table_info(signals)`. If `metadata_json` is absent:

```sql
ALTER TABLE signals ADD COLUMN metadata_json TEXT NOT NULL DEFAULT ''
```

**Steps:**
1. Write test using an old in-memory/file database schema without `metadata_json`.
2. Call `RadarStore.init()` and verify column exists.
3. Save/read a signal with metadata.
4. Run: `python3 -m pytest tests/test_storage_v03.py tests/test_storage_and_report.py -q`.
5. Commit: `feat: persist signal metadata`.

---

## Task 6: Update Formatter for New Sections

**Objective:** Show Smart Money and Resonance sections in Telegram reports.

**Files:**
- Modify: `src/banker_radar/alerts/formatter.py`
- Test: `tests/test_formatter_v03.py`

**Section order:**

```python
[
  "🧬 链上链下共振榜",
  "🧠 链上聪明钱榜",
  "🔥 轧空/追多榜",
  "🎯 暗流/埋伏榜",
  "📊 综合异动榜",
]
```

**Mapping:**
- `链上链下共振` -> `🧬 链上链下共振榜`
- `链上聪明钱`, `鲸鱼介入`, `已出货风险` -> `🧠 链上聪明钱榜`

**Steps:**
1. Write formatter test with five signals and assert section order.
2. Implement mapping/order changes.
3. Run: `python3 -m pytest tests/test_formatter_v03.py tests/test_storage_and_report.py -q`.
4. Commit: `feat: format smart money report sections`.

---

## Task 7: Wire Smart Money Into `run_scan`

**Objective:** Integrate collection, scoring, and resonance into the existing CLI scan path.

**Files:**
- Modify: `src/banker_radar/cli.py`
- Test: `tests/test_cli_v03.py`

**Behavior:**
- Default: if `smart_money.enabled: true`, fetch Smart Money and heat data.
- CLI flag: `--no-smart-money` disables it.
- On collector exception: continue with market-only signals.
- Title should become `庄家雷达 v0.3 扫描`.

**Suggested helper:**

```python
def build_smart_money_signals(cfg: dict) -> list[RadarSignal]: ...
```

**Steps:**
1. Add parser flag `--no-smart-money`.
2. Add test monkeypatching collector output and asserting report contains new sections.
3. Add test monkeypatching collector exception and asserting scan still returns report.
4. Implement wiring.
5. Run: `python3 -m pytest tests/test_cli_v03.py tests/test_collectors_and_cli.py -q`.
6. Commit: `feat: wire smart money into scan`.

---

## Task 8: Add Config Defaults

**Objective:** Add v0.3 config sections with safe defaults.

**Files:**
- Modify: `configs/radar.yaml`
- Test: `tests/test_config_v03.py`

**Steps:**
1. Write test loading config and checking `smart_money.enabled` and `resonance.enabled` exist.
2. Add config sections shown above.
3. Run: `python3 -m pytest tests/test_config_v03.py tests/test_config_env.py -q`.
4. Commit: `chore: add v0.3 config defaults`.

---

## Task 9: Update Docs and Changelog

**Objective:** Document v0.3 behavior and operational commands.

**Files:**
- Modify: `README.md`
- Modify: `ROADMAP.md`
- Modify: `CHANGELOG.md`

**Docs to add:**

```bash
banker-radar scan --db data/radar.db
banker-radar scan --no-smart-money --db data/radar.db
banker-radar telegram-send --db data/radar.db
```

Mention:
- Smart Money is best-effort.
- Collector failure degrades to market-only scan.
- Token/audit data is used for risk filtering, not investment advice.

**Steps:**
1. Update docs.
2. Run: `python3 -m pytest -q`.
3. Commit: `docs: document v0.3 smart money radar`.

---

## Task 10: End-to-End Verification

**Objective:** Verify v0.3 works locally and with Telegram dry-run.

**Files:**
- No code files unless bugs are found.

**Commands:**

```bash
cd /root/banker-radar
python3 -m pytest -q
banker-radar scan --db data/v03-e2e.db
banker-radar scan --no-smart-money --db data/v03-market-only.db
banker-radar telegram-send --dry-run --db data/v03-telegram-dryrun.db
```

**Expected:**
- Tests pass.
- Market-only scan returns current v0.2-compatible report.
- Smart Money scan either includes new sections or gracefully reports only market sections if collector unavailable.
- Dry-run prints report and does not send Telegram message.

**Commit:**

```bash
git status --short
git log --oneline -5
```

If clean and all tests pass, tag candidate:

```bash
git tag v0.3.0-rc1
```

Do not push/tag without user confirmation.

---

## Development Order Summary

1. Models
2. Binance Web3 parser/collector
3. Smart Money scoring
4. Resonance merge engine
5. SQLite metadata migration
6. Formatter sections
7. CLI integration
8. Config defaults
9. Docs/changelog
10. E2E verification

---

## Risk Notes

- Binance Web3 component output format may differ from expectation. Keep parser tolerant and test with captured examples.
- Some on-chain tokens do not map cleanly to futures symbols. v0.3 should skip unmatched tokens instead of guessing aggressively.
- Too many Smart Money alerts may spam Telegram. Keep `smart_money.min_score` and section top-N strict at first.
- High exitRate and high audit risk must reduce/disable positive signals.
- Do not put Telegram bot token, Binance credentials, private keys, or DB snapshots in git.
